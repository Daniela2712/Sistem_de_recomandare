name: sistem_de_recomandare
description: Licenta 2021-2022

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_google_places: ^0.3.0
  geocode: ^1.0.1
  uuid: 3.0.5
  dio: ^4.0.0
  google_maps_flutter: ^2.0.2
  cupertino_icons: 1.0.0
  firebase_core: ^1.10.0
  cloud_firestore: ^3.1.0
  get_it: ^7.2.0
  moor_flutter: ^4.0.0
  path_provider: ^2.0.8
  sqlite3_flutter_libs: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^4.6.0+1
  build_runner: ^2.1.7

flutter:
  uses-material-design: true
