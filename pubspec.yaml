name: sistem_de_recomandare
description: Licenta 2021-2022

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_google_places: ^0.3.0
  geocode: ^1.0.1
  uuid: ^3.0.6
  dio: ^4.0.0
  google_maps_flutter: ^2.0.2
  cupertino_icons: ^1.0.4
  firebase_core: ^1.10.0
  cloud_firestore: ^3.1.0
  get_it: ^7.2.0
  moor_flutter: ^4.0.0
  path_provider: ^2.0.8
  sqlite3_flutter_libs: ^0.5.7
  geocoding: ^2.0.2
  amadeus_flutter: ^0.0.1+1
  intl: ^0.17.0
  flutter_login: ^3.2.0
  color: 3.0.0
  shared_preferences: ^2.0.13
  email_validator: ^2.0.1
  string_validator: ^0.3.0
  image_picker: ^0.8.4+1
    # Allows you to easily use any of the 977 fonts
  google_fonts: ^2.3.2
    # Allow us to use .svg file
  flutter_svg: ^1.0.0
  flutter_rating_bar: ^4.0.0
  http: ^0.13.4
  html: ^0.15.0
  datetime_picker_formfield: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^4.6.0+1
  build_runner: ^2.1.7
  flutter_launcher_icons: ^0.9.3
flutter_icons:
  android: "icons"
  image_path: "assets/icons/travel_lunch.png"
flutter:
  uses-material-design: true

  assets:
      - assets/images/
      - assets/icons/